.env
node_modules/
dist/
build/
*.log
.DS_Store

# Product dev project artifacts — per-project state, not framework code.
# Commit for team continuity, gitignore if artifacts contain sensitive content.
# .product-dev/
